apply plugin: "war"

bootWar {
    mainClass = "com.miraway.mss.FileManagerApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
